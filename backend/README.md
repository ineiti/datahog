# Backend

Stores the data on disk, and communicates with the frontend.
Should be as small as possible, so it can easily be replaced
by danu or other backends.
Currently it uses `rocket` for the server.
