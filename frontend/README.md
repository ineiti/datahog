# Frontend

Presents the datahog interface to the user.
It uses the [datahog-npm](../npm-wasm) library and communicates
directly with the [backend](../backend).
Hopefully in a near future it uses [danu](https://danu.li) for storage
and serving the frontend.
