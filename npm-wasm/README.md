# NPM-Wasm

The wrapper around [datahog](../datahog) and the [backend](../backend) API,
usable as an npm module by the frontend.
There should be no logic in this directory, only automatic scripts
taking things from datahog and the backend.
