/**
 * Editor constants and magic values
 */

/**
 * Zero-width space character used to preserve empty content spans in the DOM
 * This ensures contentEditable elements maintain their structure even when empty
 */
export const ZERO_WIDTH_SPACE = "\u200B";
